// address stores the address of the another variable


#include<stdio.h>
#include<string.h>

int main()
{
        int a = 10;
        printf("value of a = %d\n", a);

        printf("Address of a = %x", &a);
        
    return 0;
}